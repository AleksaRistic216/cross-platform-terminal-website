Start the Next.js dev server so the user can view the site locally in their browser.

Run `npm run dev` in the repo root using `run_in_background: true`. Do not use the built-in /run skill.

After launching, tell the user the site is available at http://localhost:3000.
